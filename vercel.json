{
  "rootDirectory": "backend",
  "installCommand": "npm install",
  "buildCommand": "npm run generate && npm run build",
  "functions": {
    "api/[[route]].ts": {
      "maxDuration": 30,
      "memory": 512
    }
  }
}
