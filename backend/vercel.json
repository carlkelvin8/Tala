{
  "installCommand": "npm install",
  "buildCommand": "npm run generate && npm run build",
  "functions": {
    "api/[[route]].js": {
      "maxDuration": 30
    }
  },
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/[[route]]" },
    { "src": "/", "dest": "/api/[[route]]" }
  ]
}